<%= form_with url: admin_items_path, method: :get do |f| %>
  <div class="row">
    <div><%= f.text_field :word, placeholder: "商品検索" %></div>
    <div><%= f.submit "検索" %></div>
  </div>
  <% unless @serch_counts.nil? %>
    <b class="row mt-3 text-white">⇒　"<%= @serch_word %>"で<%= @serch_counts %>件ヒット</b>
  <% end %>
<% end %>