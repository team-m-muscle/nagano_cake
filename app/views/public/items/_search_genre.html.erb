<table class="table table-hover border">
  <tr>
    <th class="bg-light">ジャンル検索</th>
  </tr>
  <% @genres.each do |genre| %>
    <tr><td><%= link_to genre.name, genre_search_path(genre) %><br> </td></tr>
  <% end %>
</table>