ジャンル検索<br>
<% @genres.each do |genre| %>
  <%= link_to genre.name, genre_search_path(genre) %><br>
<% end %>

<!--%= form_with url: search_path, method: :get do |f| %>-->
  <!--%= f.collection_check_boxes :tag_ids, Tag.all, :id, :name do |b| %>-->
    <!--%= b.check_box %>-->
    <!--%= b.label %>-->
  <!--% end %>-->
  <!--%= f.submit "検索" %>-->
<!--% end %>-->